# 直接访问开发服务器

如果浏览器一直显示"重定向中",请按以下步骤操作:

## 步骤1: 检查命令行窗口

查看启动的两个命令行窗口:

### 后端窗口
应该显示类似:
```
Flask app running on http://localhost:5001
```

### 前端窗口  
应该显示类似:
```
Compiled successfully!
You can now view youthgain-frontend in the browser.
Local: http://localhost:3000
```

## 步骤2: 如果前端窗口卡住

### 情况A: 显示"正在编译..."
说明还在编译,继续等待

### 情况B: 显示错误信息
1. 按 Ctrl+C 停止
2. 删除 node_modules 文件夹
3. 重新运行: `npm install`
4. 再运行: `npm start`

### 情况C: 没有任何输出
可能卡住了,按 Ctrl+C 停止,重新运行

## 步骤3: 手动访问

不要等浏览器自动打开,直接在浏览器地址栏输入:

```
http://localhost:3000
```

## 步骤4: 如果还是不行

### 方法1: 清除缓存重新安装
```bash
# 停止所有服务
# 删除缓存
rmdir /s /q node_modules
del package-lock.json

# 重新安装
npm install

# 启动
npm start
```

### 方法2: 使用生产模式
如果开发模式一直有问题,使用生产模式:

1. 构建项目:
```bash
npm run build
```

2. 运行 `快速启动.cmd`

3. 访问 http://localhost:3000

## 常见问题

### Q: 为什么一直重定向?
A: 可能是:
- React编译卡住
- 端口3000被其他程序占用
- node_modules损坏

### Q: 如何确认服务是否启动?
A: 运行命令检查:
```bash
netstat -ano | findstr ":3000"
```
如果有输出,说明服务在运行

### Q: 编辑按钮在哪里?
A: 成功启动后:
- 登录进入Dashboard
- 查看"储蓄目标"和"本月预算"卡片
- 右上角有铅笔图标

## 需要帮助?

请提供:
1. 前端命令行窗口的完整输出
2. 后端命令行窗口的完整输出  
3. 浏览器控制台(F12)的错误信息
