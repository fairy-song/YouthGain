# 青盈 YouthGain — 管理员系统使用说明

为「青盈」新增的管理员系统：独立的管理员角色账号，登录后进入管理后台，可查看平台统计、管理全部用户、管理知识库内容。普通用户访问管理接口一律返回 403。

## 一、管理员账号

| 环境 | 账号 | 说明 |
|---|---|---|
| 开发模式（`DEV_MODE=true`） | `admin@youthgain.com`（默认） | 本地认证绕过开启，但**角色仍按邮箱判定**：只有命中 `ADMIN_EMAILS` 的邮箱是管理员，其他邮箱登录为普通用户（访问 `/admin` 会被拒） |
| 生产模式（真实 Firebase） | `admin@youthgain.com`（默认） | 需在 Firebase 控制台用该邮箱注册账号；邮箱命中 `ADMIN_EMAILS` 即自动获得管理员角色 |

**如何更换管理员邮箱**：修改根目录 `.env.local`（或生产环境 `.env`）中的

```
ADMIN_EMAILS=admin@youthgain.com
```

多个管理员用英文逗号分隔：`ADMIN_EMAILS=a@x.com,b@x.com`。改完重启后端生效。

## 二、管理后台入口

1. 启动服务（`快速启动.cmd` 或手动启动前后端）。
2. 登录后导航栏出现黄色的「**管理后台**」入口（普通用户看不到）；管理员登录成功默认跳转到 `/admin`。
3. 管理后台包含三个 Tab：
   - **平台统计**：注册用户数、完成评估用户数、评估记录数、理财成长记录数、消费记录数、理财目标数、AI 教练对话数、知识库文章数，支持手动刷新。
   - **用户管理**：用户列表（搜索邮箱/昵称/UID）、用户详情（含业务数据量）、**停用/启用**（停用后该用户所有接口被拒绝）、**删除**（连同评估/目标/消费/学习记录一并删除，不可恢复）。
   - **知识库管理**：文章的**新增 / 编辑 / 删除**（标题、分类、标签、摘要、正文 Markdown、封面图、阅读时长、发布日期）。

> 自我保护：管理员不能删除「当前登录的」管理员账号，防止误删唯一管理员。

## 三、后端接口清单（`/api/admin/*`）

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/admin/users` | 用户列表 |
| GET | `/api/admin/users/<uid>` | 用户详情（含各业务数据量） |
| PUT | `/api/admin/users/<uid>/status` | 停用/启用 `{ "disabled": true\|false }` |
| DELETE | `/api/admin/users/<uid>` | 删除用户及全部业务数据 |
| GET | `/api/admin/stats` | 平台统计 |
| GET | `/api/admin/kb` | 知识库文章列表 |
| POST | `/api/admin/kb` | 新增文章 |
| PUT | `/api/admin/kb/<id>` | 更新文章 |
| DELETE | `/api/admin/kb/<id>` | 删除文章 |

另：`GET /api/auth/me` 现在会返回 `role` 字段（`admin` / `user`），前端据此显示/隐藏管理入口。

## 四、权限实现方式

- **后端**：`auth_service.require_admin` 装饰器在 `require_auth` 之上校验角色——邮箱命中 `ADMIN_EMAILS` 的管理员放行，普通用户 403，未登录 401。开发模式（`DEV_MODE=true`）同样按邮箱判定：前端登录/刷新时携带 `X-Dev-Email` 请求头，后端据此区分管理员与普通用户，行为与生产一致。
- **停用拦截**：`verify_firebase_token` 读到用户档案 `disabled=true` 时拒绝该用户的一切请求。
- **前端**：`AuthContext` 新增 `isAdmin`，登录后由后端 `/api/auth/me` 权威判定；`/admin` 路由受 `AdminRoute` 保护，普通用户访问自动跳回个人中心。

## 五、存储支持

内存模式（`dev_data.json`）、MySQL 模式、Firestore 模式均已实现用户管理/统计/知识库 CRUD；MySQL 下知识库首次使用时自动建表 `kb_articles`，`users` 表自动补 `disabled` 列。

## 六、涉及文件

**后端**
- `backend/app/config.py`（新增 `ADMIN_EMAILS`）
- `backend/app/services/auth_service.py`（管理员识别 + `require_admin`）
- `backend/app/services/admin_service.py`（新增：用户/统计/知识库数据访问）
- `backend/app/routes/admin_routes.py`（新增：管理员 API）
- `backend/app/routes/auth_routes.py`（`/me` 返回角色）
- `backend/app/__init__.py`（注册 `auth_bp`、`admin_bp`）
- `.env.local` / `.env.example`（`ADMIN_EMAILS` 配置）

**前端**
- `src/services/api.js`（管理员 API 调用）
- `src/contexts/AuthContext.js`（`isAdmin` 角色状态）
- `src/pages/AdminDashboard.js`（新增：管理后台页面）
- `src/App.js`（`/admin` 路由 + `AdminRoute`）
- `src/components/Layout.js` / `src/components/MobileNavBar.js`（管理员导航入口）
- `src/pages/Login.js`（管理员登录跳转 `/admin`）
