# Flask 框架及扩展
flask>=3.0.0
flask-cors>=4.0.0

# 环境变量管理
python-dotenv>=1.0.0

# Firebase Admin SDK
firebase-admin>=6.5.0

# Google Generative AI (如果使用 Gemini)
google-generativeai>=0.3.1

# HTTP 请求库
requests>=2.31.0

# WSGI 服务器
gunicorn>=21.2.0

# 智谱 AI SDK
zhipuai>=2.1.5

# 讯飞语音听写 WebAPI 客户端（xfyun_asr.py 依赖）
# 此前以 --target 方式 vendor 在 backend/vendor_pkgs/，未声明依赖，
# 导致克隆仓库后语音录入功能无法运行。现改为正常声明。
websocket-client>=1.9.0

# MySQL 数据库驱动及加密支持
PyMySQL>=1.1.0
cryptography>=41.0.0